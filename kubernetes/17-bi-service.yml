apiVersion: apps/v1
kind: Deployment
metadata:
  name: bi-service
  namespace: crm-supermercado
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bi-service
  template:
    metadata:
      labels:
        app: bi-service
    spec:
      containers:
        - name: bi-service
          image: crm/bi-service:latest
          ports:
            - containerPort: 3012
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3012"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: bi-service
  namespace: crm-supermercado
spec:
  selector:
    app: bi-service
  ports:
    - port: 3012
      targetPort: 3012
