apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdv-service
  namespace: crm-supermercado
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pdv-service
  template:
    metadata:
      labels:
        app: pdv-service
    spec:
      containers:
        - name: pdv-service
          image: crm/pdv-service:latest
          ports:
            - containerPort: 3004
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: pdv-service
  namespace: crm-supermercado
spec:
  selector:
    app: pdv-service
  ports:
    - port: 3004
      targetPort: 3004
