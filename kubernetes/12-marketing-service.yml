apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-service
  namespace: crm-supermercado
spec:
  replicas: 2
  selector:
    matchLabels:
      app: marketing-service
  template:
    metadata:
      labels:
        app: marketing-service
    spec:
      containers:
        - name: marketing-service
          image: crm/marketing-service:latest
          ports:
            - containerPort: 3007
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3007"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: marketing-service
  namespace: crm-supermercado
spec:
  selector:
    app: marketing-service
  ports:
    - port: 3007
      targetPort: 3007
