apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchasing-service
  namespace: crm-supermercado
spec:
  replicas: 2
  selector:
    matchLabels:
      app: purchasing-service
  template:
    metadata:
      labels:
        app: purchasing-service
    spec:
      containers:
        - name: purchasing-service
          image: crm/purchasing-service:latest
          ports:
            - containerPort: 3009
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3009"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: purchasing-service
  namespace: crm-supermercado
spec:
  selector:
    app: purchasing-service
  ports:
    - port: 3009
      targetPort: 3009
