apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-service
  namespace: crm-supermercado
spec:
  replicas: 2
  selector:
    matchLabels:
      app: security-service
  template:
    metadata:
      labels:
        app: security-service
    spec:
      containers:
        - name: security-service
          image: crm/security-service:latest
          ports:
            - containerPort: 3008
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3008"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: security-service
  namespace: crm-supermercado
spec:
  selector:
    app: security-service
  ports:
    - port: 3008
      targetPort: 3008
